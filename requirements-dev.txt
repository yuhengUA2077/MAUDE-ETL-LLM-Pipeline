# Development dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
nbstripout>=0.7        # strip notebook outputs before committing
